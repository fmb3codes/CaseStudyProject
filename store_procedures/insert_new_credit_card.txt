CREATE OR REPLACE PROCEDURE SP_INSERT_NEW_CARD
(C_NAME VARCHAR2, C_NUM VARCHAR2, C_TYPE VARCHAR2)
AS
BEGIN
  INSERT INTO CREDIT_CARDS(CC_ID, NAME_ON_CARD, CARD_NUMBER, CT_ID)
  VALUES(SEQ_CREDIT_CARDS.NEXTVAL,C_NAME,C_NUM,C_TYPE);
END;
