CREATE OR REPLACE PROCEDURE SP_INSERT_NEW_CL
(LT_ID CHAR, SA VARCHAR2, CITY VARCHAR2, STATE VARCHAR2, ZIP VARCHAR,
C_ID CHAR)
AS
BEGIN
  INSERT INTO CUSTOMERS_LOCATIONS(CL_ID,LT_ID,STREET_ADDRESS,
  CITY,STATE,ZIP_CODE,C_ID)
  VALUES(SEQ_CUS_LOC.NEXTVAL,LT_ID,SA,CITY,STATE,ZIP,C_ID);
END;